#!/usr/bin/env bash
set -u

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root" || exit 1
tools/verify-control-plane-release | grep -q 'READY_FOR_PRODUCTION_GATE' || exit 1
printf 'control-plane release contract: PASS\n'
