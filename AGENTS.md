# AI OS V2 공용 작업 규칙

## 새 작업 및 새 세션 시작 규칙

- 새 세션에서는 다른 문서보다 `wiki/SESSION_START_CONTRACT.md`를 가장 먼저 읽고 그 절차와 보고 형식을 적용한다.
- 새 작업 또는 새 세션에서 사용자의 첫 요청을 처리하기 전에 현재 저장소와 `main` 상태를 확인한다.
- 원격 확인 전에 로컬 미커밋 변경이나 충돌 위험이 있으면 동기화하거나 덮어쓰지 말고 중단하여 사용자에게 보고한다.
- 작업트리가 안전할 때 원격 `origin/main`을 확인하고 필요한 경우 fast-forward 방식으로만 최신 상태를 동기화한다.
- 현재 HEAD Commit SHA를 `Base Memory`로 기록하고, 반드시 실제 파일에서 다음 문서를 읽는다.
  - `wiki/GOAL.md`
  - `wiki/CURRENT_STATE.md`
  - `wiki/DECISIONS.md`
  - `wiki/ARCHITECTURE.md`
  - `wiki/VERIFICATION.md`
- 읽지 않은 내용을 기억이나 추측으로 대체하지 않는다.
- 위 Wiki 문서를 AI OS V2의 공용 기억으로 사용하며, 채팅의 추측보다 승인된 Git commit의 내용을 우선한다.
- 로컬 Worktree·GitHub `origin/main`·Obsidian Vault의 HEAD를 각각 확인하고, SHA가 다르면 `동기화됨`이라고 표현하지 않는다.
- 필수 문서를 모두 읽은 경우에만 `AI OS V2 Memory Loaded: <Commit SHA>` 형식으로 보고한 뒤 작업을 시작한다. 일부에 접근하지 못했으면 누락을 보고하고 이 문구를 사용하지 않는다.
- 작업 중 `main`이 변경되어도 `Base Memory`를 임의로 바꾸지 않는다. 새 Commit이 필요하면 다시 Load하여 새 작업으로 처리한다.
- Post-MVP PM 작업에서는 `wiki/PM1_PM4_DESIGN_MEETING_2026-08-19.md`도 읽고, 그 안의 확정 용어·PM 전환·Skill 보호·수집기 규칙을 적용한다.
- V2의 기능·상태·PASS·동기화·복구를 주장하기 전 `wiki/AI_EVIDENCE_GUARD.md`를 읽고 주장에 필요한 실제 증거를 확인한다.
- Antigravity는 `.agents/rules/evidence-first.md`를 Workspace Rule로 사용하며 Codex와 같은 증거 우선 정책을 따른다.

## 저장 규칙

- 확정된 결정이나 검증 결과는 `작업 → 검증 → 사용자 승인 → Wiki·세션 공통계약 반영 → Commit → Push` 순서로 저장한다.
- PM이 끝나거나 Repo-local Skill을 만들거나 변경하면 `wiki/SESSION_START_CONTRACT.md`의 정확한 상태와 Skill 목록을 같은 Commit에서 갱신한다.
- 세션 공통계약 반영이 누락되면 PM 또는 Skill 저장 완료로 보고하지 않는다.
- 검증되지 않은 내용을 확정 사실로 저장하지 않는다.
- 한 Commit에는 가능한 하나의 의미 있는 변경만 포함한다.
- Commit과 Push가 완료되면 `Saved: <Commit SHA>` 형식으로 보고한다.

## 공통 상태 언어

- 💡 제안: 아이디어 또는 후보이며 아직 구현되지 않은 상태다.
- 📝 승인됨: 사용자가 채택했지만 아직 구현되지 않은 상태다.
- 🔨 구현됨: 실제 코드 또는 설정이 존재하지만 동작은 아직 검증되지 않은 상태다.
- ✅ 검증됨: 실제 동작을 확인했고 그 증거가 존재하는 상태다.

- 구현 전에는 `구현됨`이라고 표현하지 않는다.
- 실제 검증 전에는 `PASS`, `완료`, `검증됨`이라고 표현하지 않는다.
- 직접 확인하지 못한 내용은 `확인 필요`라고 표현한다.

## 초보자 대화 및 회의 규칙

- 사용자는 초보자이며 기술 용어를 알고 있다고 가정하지 않는다.
- 먼저 쉬운 자연어로 설명하고, 꼭 필요한 경우에만 기술 용어를 괄호 안에 덧붙인다.
- 사용자가 결정해야 할 설계·범위·비용·보안·외부 전송·복구 문제가 생기면 임의로 결정하지 말고 `회의할 항목이 있습니다`라고 먼저 알린다.
- 회의에서는 한 번에 한 주제만 설명하고 사용자 결정을 기록한다.
- 지금 필요하지 않은 미래 설계를 회의 안건으로 강제하지 않는다.
- 회의가 끝나면 `회의가 끝났습니다`라고 명확히 알리고 확정·보류·미결 항목을 기록한다.
- 사용자의 표현을 그대로 기술 작업으로 실행하기 전에 `사용자 표현 → V2가 이해한 작업 → 변경 범위 → 유지 범위 → 확인 방법`으로 짧게 번역해 보여준다.
- 기술 결과를 사용자에게 전달할 때는 `무엇이 달라졌는지 → 사용자가 할 일 → 문제 시 되돌리는 방법` 순서로 설명한다.
- 초보자 요청이나 기술 결과를 설명할 때 `.agents/skills/v2-beginner-translator/SKILL.md`를 사용한다.
- 영어 기술 용어는 필요한 경우에만 `English (한글 발음) — 쉬운 역할` 형식으로 보여주고, 사용자가 선택하면 역할·사용 시점·현재 화면에서의 의미를 추가 설명한다.

## V2 기본 용어

- `V2`: Skill을 사용해 결과물을 제작하고 Module을 장착·관리하는 보드판이다. V2 자체를 Module이라고 부르지 않는다.
- `프로젝트`: 사용자가 제작 작업을 진행하는 작업공간이다. 프로젝트 자체를 Module이라고 부르지 않는다.
- `Section`: 요구사항 대화·현재 단계·Preview처럼 프로젝트 화면을 구성하는 영역이다.
- `Skill`: V2가 작업에 사용하는 방법이나 도구다. 사용자 화면에서는 `스킬`로 표현하며 `능력`이라는 이름으로 바꾸지 않는다.
- `Module`: V2가 제작했거나 가져와 검증했으며 장착·제거·재사용할 수 있는 실제 결과물이다.
- 카톡형 요구사항 대화창과 Preview는 프로젝트 화면의 `Section`이며 Module이 아니다.
- V2의 제품 정체성은 ERP가 아니라 OS형 제작·운영 보드판이다. ERP는 V2가 만들 수 있는 자식 제품 중 하나다.

## PM 전환 규칙

- 사용자가 `PM 통과`를 결정하면 승인 화면·핵심 상호작용·유지 범위·변경 범위·복구 Commit을 자동 정리한다.
- 완료 PM을 Commit·Tag·Hash로 잠그고 다음 PM 전용 작업공간을 만든다.
- 다음 PM에서는 이전 PM 잠금 검사를 먼저 수행하고 회귀가 있으면 진행을 중단한다.
- 승인 기준 Screenshot이나 잠금 Hash를 테스트 통과 목적으로 임의 갱신하지 않는다.
- 사용자는 정답지를 직접 작성하지 않으며 최종 `통과` 여부만 결정한다.
- 작은 수정이 새 Section·새 편집 기능·외부 도구·여러 화면으로 커지면 즉시 멈추고
  짧은 인터뷰로 전환한다. 확인된 Interview Receipt가 없으면 확장 구현을 시작하지 않는다.
- PM3은 편집 기능의 실제 동작을 판정하고, PM6은 실제 고객 결과물·모바일·Undo·원본
  보존·프로젝트 격리·접근성·회귀를 다시 판정한다. PM3 PASS를 PM6 PASS로 대체하지 않는다.

## 수집기 및 Skill 연결 규칙

- 사용자가 작업을 입력하면 수집기를 시작한다.
- 현재 프로젝트 파일·과거 승인 결과·기존 Skill·Recipe·Block을 먼저 확인하고 부족한 부분만 외부 조사한다.
- 외부 Skill·Plugin·Library는 원본을 직접 수정하지 않고 격리 시험 후 제거 가능한 Adapter로 연결한다.
- 기존 Skill과 중복되는지, 시간·품질·오류·재작업이 실제로 좋아지는지 비교한다.
- 사용자가 `채택`하면 즉시 기본 Skill로 적용하되 이전 Version은 복구용으로 보존한다.
- 비공개 프로젝트·V2 Core·Secret·Token·Cookie를 외부에 자동 전송하지 않는다.
- 외부 전송이 필요하면 전송 자료·목적·보관 여부를 쉬운 말로 보여주고 사용자 승인을 받는다.
